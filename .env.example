# Copy to `.env` and fill in your credentials for local development
# Do NOT commit your real `.env` to version control

# Gmail account used to send emails (or another SMTP address)
EMAIL_USER=your.email@example.com

# An app password or SMTP password for the account (Gmail often requires an app password)
EMAIL_PASS=your-email-password-or-app-password

# The address that will receive contact form submissions
EMAIL_TO=mmagilavan@gmail.com

# Optional: the authenticated "from" address (defaults to EMAIL_USER)
# EMAIL_FROM=your.email@example.com

# Optional: set to "development" during local testing so server returns helpful errors
# NODE_ENV=development
